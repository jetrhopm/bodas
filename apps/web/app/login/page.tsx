'use client'; import {useState} from 'react'; import { apiUrl } from '../api';
export default function Login(){const [error,setError]=useState('');const [email,setEmail]=useState('admin@local.test');const [password,setPassword]=useState('password');async function submit(e:React.FormEvent){e.preventDefault();setError('');const r=await fetch(`${apiUrl}/auth/login`,{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});if(!r.ok){setError('No pudimos iniciar sesión. Revisa tus datos.');return} const d=await r.json();sessionStorage.setItem('accessToken',d.accessToken);sessionStorage.setItem('user',JSON.stringify(d.user));location.href=`${process.env.NEXT_PUBLIC_BASE_PATH||''}${d.user.role==='COUPLE'?'/portal':'/panel'}`}return <main className="grid min-h-screen place-items-center p-5"><div className="grid w-full max-w-5xl overflow-hidden rounded-[2rem] border border-white/80 bg-white shadow-[0_25px_70px_rgba(60,42,28,.14)] md:grid-cols-[.9fr_1.1fr]"><aside className="hidden min-h-[590px] flex-col justify-between bg-[linear-gradient(145deg,#36533e,#6f8a6b)] p-10 text-white md:flex"><div><p className="text-xs font-bold tracking-[.24em] text-[#e7c993]">AUREA STUDIO</p><h1 className="mt-10 font-serif text-5xl leading-[1.04]">Cada detalle cuenta una historia.</h1><p className="mt-6 max-w-xs text-sm leading-6 text-white/75">Planeación de bodas con calma, claridad y una experiencia cuidada para cada pareja.</p></div><p className="text-xs tracking-widest text-white/60">PLANEACIÓN · OPERACIÓN · CELEBRACIÓN</p></aside><form onSubmit={submit} className="p-7 sm:p-10"><p className="eyebrow">ACCESO PRIVADO</p><p className="mt-5 font-serif text-4xl">Bienvenida de vuelta</p><p className="mt-3 text-sm leading-6 text-stone-500">Ingresa al espacio de trabajo de Aurea o al portal de tu celebración.</p><label className="mt-9 block text-sm font-semibold text-stone-700">Correo<input value={email} onChange={e=>setEmail(e.target.value)} type="email" className="mt-2 w-full rounded-xl border p-3" required/></label><label className="mt-5 block text-sm font-semibold text-stone-700">Contraseña<input value={password} onChange={e=>setPassword(e.target.value)} type="password" className="mt-2 w-full rounded-xl border p-3" required/></label>{error&&<p role="alert" className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button className="primary-button mt-7 w-full rounded-xl p-3.5 font-semibold">Iniciar sesión <span className="ml-2">→</span></button><p className="mt-6 text-center text-xs text-stone-400">Acceso protegido para equipo y parejas autorizadas.</p></form></div></main>}
