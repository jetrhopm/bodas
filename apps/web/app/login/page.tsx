'use client'; import {useState} from 'react';
export default function Login(){const [error,setError]=useState('');const [email,setEmail]=useState('admin@demo.mx');const [password,setPassword]=useState('Desarrollo123!');async function submit(e:React.FormEvent){e.preventDefault();setError('');const r=await fetch(`${process.env.NEXT_PUBLIC_API_URL||'http://localhost:3001/api/v1'}/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});if(!r.ok){setError('No pudimos iniciar sesión. Revisa tus datos.');return} const d=await r.json();sessionStorage.setItem('accessToken',d.accessToken);location.href='/panel'}return <main className="grid min-h-screen place-items-center p-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-sm"><p className="font-serif text-3xl">Bienvenida de vuelta</p><p className="mt-2 text-sm text-stone-500">Acceso seguro para el equipo de Aurea.</p><label className="mt-7 block text-sm">Correo<input value={email} onChange={e=>setEmail(e.target.value)} type="email" className="mt-2 w-full rounded-xl border p-3" required/></label><label className="mt-4 block text-sm">Contraseña<input value={password} onChange={e=>setPassword(e.target.value)} type="password" className="mt-2 w-full rounded-xl border p-3" required/></label>{error&&<p role="alert" className="mt-3 text-sm text-red-700">{error}</p>}<button className="mt-6 w-full rounded-xl bg-sage p-3 text-white">Iniciar sesión</button></form></main>}
