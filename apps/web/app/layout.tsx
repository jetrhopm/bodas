import './globals.css'; import type { Metadata } from 'next'; import SessionRefresher from './session-refresher'; export const metadata:Metadata={title:'Aurea · Planeación de bodas',description:'Planeación cuidada, momentos inolvidables'}; export default function Layout({children}:{children:React.ReactNode}){return <html lang="es-MX"><body><SessionRefresher/>{children}</body></html>}
