'use client';
import {useState} from 'react'; import {apiUrl as api} from '../../api';
export default function NewWedding(){const [msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const data:any=Object.fromEntries(f);if(data.estimatedGuests)data.estimatedGuests=Number(data.estimatedGuests);const r=await fetch(`${api}/weddings`,{method:'POST',headers:{Authorization:`Bearer ${sessionStorage.getItem('accessToken')}`,'Content-Type':'application/json'},body:JSON.stringify(data)});if(!r.ok){setMsg('No se pudo guardar');return}const w=await r.json();location.href=`${process.env.NEXT_PUBLIC_BASE_PATH||''}/panel/bodas/${w.id}`}return <main className="mx-auto max-w-2xl p-6 md:p-10"><p className="text-xs tracking-[.2em] text-rose">NUEVA BODA</p><h1 className="mt-2 font-serif text-4xl">Pareja y celebración</h1><form onSubmit={submit} className="mt-6 grid gap-4 rounded-3xl bg-white p-6"><input className="rounded-xl border p-3" name="title" placeholder="Nombre visible: Ana & Luis" required/><div className="grid gap-3 md:grid-cols-2"><input className="rounded-xl border p-3" name="partnerOneName" placeholder="Nombre integrante 1" required/><input className="rounded-xl border p-3" name="partnerTwoName" placeholder="Nombre integrante 2"/></div><div className="grid gap-3 md:grid-cols-2"><input className="rounded-xl border p-3" name="email" type="email" placeholder="Correo de contacto"/><input className="rounded-xl border p-3" name="phone" placeholder="Teléfono"/></div><div className="grid gap-3 md:grid-cols-2"><input className="rounded-xl border p-3" name="date" type="datetime-local"/><input className="rounded-xl border p-3" name="estimatedGuests" type="number" min="1" placeholder="Invitados estimados"/></div><input className="rounded-xl border p-3" name="style" placeholder="Estilo o temática"/><button className="rounded-xl bg-sage p-3 text-white">Crear boda y expediente</button>{msg&&<p className="text-red-700">{msg}</p>}</form></main>}
