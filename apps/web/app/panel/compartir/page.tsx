'use client';
import {useState} from 'react';
export default function Share(){const [name,setName]=useState('');const [couple,setCouple]=useState('');const [link,setLink]=useState('');const message=`Hola, ${name||'{nombre}'}. Nos encantará compartir este día tan especial contigo. Consulta tu invitación y confirma tu asistencia aquí: ${link||'{enlace_invitacion}'}`;function open(){window.open(`https://wa.me/?text=${encodeURIComponent(message)}`,'_blank','noopener,noreferrer')}return <main className="mx-auto max-w-xl p-6 md:p-10"><p className="text-xs tracking-[.2em] text-rose">WHATSAPP MANUAL</p><h1 className="mt-2 font-serif text-4xl">Compartir invitación</h1><p className="mt-3 text-stone-600">Abrir WhatsApp no confirma entrega, lectura ni asistencia.</p><div className="mt-6 rounded-3xl bg-white p-6"><input className="w-full rounded-xl border p-3" value={name} onChange={e=>setName(e.target.value)} placeholder="Nombre del invitado"/><input className="mt-3 w-full rounded-xl border p-3" value={couple} onChange={e=>setCouple(e.target.value)} placeholder="Pareja (referencia interna)"/><input className="mt-3 w-full rounded-xl border p-3" value={link} onChange={e=>setLink(e.target.value)} placeholder="Enlace seguro de invitación"/><textarea readOnly className="mt-4 h-32 w-full rounded-xl border bg-cream p-3" value={message}/><div className="mt-4 flex gap-3"><button onClick={()=>navigator.clipboard.writeText(message)} className="rounded-xl border px-4 py-3">Copiar mensaje</button><button onClick={open} className="rounded-xl bg-sage px-4 py-3 text-white">Abrir WhatsApp</button></div></div></main>}
