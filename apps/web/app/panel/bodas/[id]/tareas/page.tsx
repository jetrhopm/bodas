'use client'; import {useEffect,useState} from 'react'; import {useParams} from 'next/navigation'; import {apiUrl as api} from '../../../../api';
export default function Tasks(){const {id}=useParams<{id:string}>();const [w,setW]=useState<any>();const h={Authorization:`Bearer ${typeof window==='undefined'?'':sessionStorage.getItem('accessToken')}`};useEffect(()=>{fetch(`${api}/weddings/${id}`,{headers:h}).then(r=>r.json()).then(setW)},[id]);if(!w)return <main className="p-8">Cargando…</main>;return <main className="p-6"><h1 className="font-serif text-4xl">Tareas</h1>{w.tasks.map((t:any)=><div className="mt-3 rounded-xl bg-white p-4" key={t.id}><b>{t.title}</b><select className="ml-4 border" value={t.status} onChange={async e=>{await fetch(`${api}/weddings/${id}/tasks/${t.id}`,{method:'PATCH',headers:{...h,'Content-Type':'application/json'},body:JSON.stringify({status:e.target.value})});location.reload()}}><option value="TODO">Pendiente</option><option value="IN_PROGRESS">En proceso</option><option value="DONE">Completada</option><option value="CANCELLED">Cancelada</option></select></div>)}</main>}
