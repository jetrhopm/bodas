import Link from 'next/link';
export default async function WeddingLayout({children,params}:{children:React.ReactNode,params:Promise<{id:string}>}){const {id}=await params;const base=`/panel/bodas/${id}`;return <><nav className="flex gap-2 overflow-auto bg-white px-5 py-3 text-sm"><Link href={base}>Boda</Link><Link href={`${base}/agenda`}>Agenda</Link><Link href={`${base}/finanzas`}>Finanzas</Link><Link href={`${base}/invitados`}>Invitados</Link><Link href={`${base}/importar-invitados`}>CSV</Link><Link href={`${base}/invitaciones`}>Invitaciones</Link><Link href={`${base}/publicar`}>Publicar</Link></nav>{children}</>}
