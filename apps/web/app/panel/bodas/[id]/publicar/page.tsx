'use client'; import {useParams} from 'next/navigation'; import {apiUrl as api} from '../../../../api';
export default function Publish(){const {id}=useParams<{id:string}>();async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();await fetch(`${api}/weddings/${id}/publish`,{method:'PATCH',headers:{Authorization:`Bearer ${sessionStorage.getItem('accessToken')}`,'Content-Type':'application/json'},body:JSON.stringify({...Object.fromEntries(new FormData(e.currentTarget)),isPublished:true})});alert('Información publicada')}return <main className="mx-auto max-w-2xl p-6"><h1 className="font-serif text-4xl">Invitación pública</h1><form onSubmit={save} className="mt-6 grid gap-3 rounded-3xl bg-white p-6"><input name="ceremonyVenue" className="rounded-xl border p-3" placeholder="Ubicación ceremonia"/><input name="receptionVenue" className="rounded-xl border p-3" placeholder="Ubicación recepción"/><input name="dressCode" className="rounded-xl border p-3" placeholder="Código de vestimenta"/><textarea name="program" className="rounded-xl border p-3" placeholder="Programa del evento"/><textarea name="giftsInfo" className="rounded-xl border p-3" placeholder="Mesa de regalos"/><input name="galleryUrl" className="rounded-xl border p-3" placeholder="URL de galería"/><button className="rounded-xl bg-sage p-3 text-white">Publicar cambios</button></form></main>}
