'use client'; import {useEffect,useState} from 'react'; import {apiUrl as api} from '../../api';
export default function Team(){const [users,setUsers]=useState<any[]>([]);const h=()=>({Authorization:`Bearer ${sessionStorage.getItem('accessToken')}`});const load=()=>fetch(`${api}/users`,{headers:h()}).then(r=>r.json()).then(setUsers);useEffect(()=>{load()},[]);async function add(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const r=await fetch(`${api}/users`,{method:'POST',headers:{...h(),'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});if(r.ok){e.currentTarget.reset();load()}}return <main className="mx-auto max-w-5xl p-6"><h1 className="font-serif text-4xl">Equipo</h1><div className="mt-6 grid gap-5 md:grid-cols-[1fr_320px]"><ul className="rounded-3xl bg-white p-6">{users.map(u=><li className="border-b py-3" key={u.id}><b>{u.name}</b><p className="text-sm">{u.email} · {u.role}</p></li>)}</ul><form onSubmit={add} className="rounded-3xl bg-white p-6"><input className="w-full rounded-xl border p-3" name="name" placeholder="Nombre" required/><input className="mt-3 w-full rounded-xl border p-3" name="email" type="email" placeholder="Correo" required/><select className="mt-3 w-full rounded-xl border p-3" name="role"><option value="PLANNER">Planner</option><option value="ASSISTANT">Asistente</option><option value="COUPLE">Pareja</option></select><input className="mt-3 w-full rounded-xl border p-3" name="password" type="password" placeholder="Contraseña temporal" required/><button className="mt-4 w-full rounded-xl bg-sage p-3 text-white">Crear usuario</button></form></div></main>}
