'use client'; import {useEffect,useState} from 'react'; import {apiUrl as api} from '../../api';
export default function Vendors(){const [items,setItems]=useState<any[]>([]);const load=()=>fetch(`${api}/vendors`,{headers:{Authorization:`Bearer ${sessionStorage.getItem('accessToken')}`}}).then(r=>r.json()).then(setItems);useEffect(()=>{load()},[]);async function add(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const r=await fetch(`${api}/vendors`,{method:'POST',headers:{Authorization:`Bearer ${sessionStorage.getItem('accessToken')}`,'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});if(r.ok){e.currentTarget.reset();load()}}return <main className="mx-auto max-w-5xl p-6"><h1 className="font-serif text-4xl">Proveedores</h1><div className="mt-6 grid gap-5 md:grid-cols-[1fr_320px]"><ul className="rounded-3xl bg-white p-6">{items.map(x=><li className="border-b py-3" key={x.id}><b>{x.name}</b><p className="text-sm text-stone-500">{x.category} · {x.contactName||'Sin contacto'}</p></li>)}</ul><form onSubmit={add} className="rounded-3xl bg-white p-6"><input name="name" className="w-full rounded-xl border p-3" placeholder="Nombre comercial" required/><input name="category" className="mt-3 w-full rounded-xl border p-3" placeholder="Categoría" required/><input name="contactName" className="mt-3 w-full rounded-xl border p-3" placeholder="Contacto"/><input name="phone" className="mt-3 w-full rounded-xl border p-3" placeholder="Teléfono"/><input name="email" className="mt-3 w-full rounded-xl border p-3" placeholder="Correo"/><button className="mt-4 w-full rounded-xl bg-sage p-3 text-white">Guardar proveedor</button></form></div></main>}
